<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The INTERNET permission is required for development. Specifically,
         the Flutter tool needs it to communicate with the running application
         to allow setting breakpoints, to provide hot reload, etc.
    -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Debug-only: allow plain-HTTP (cleartext) requests to the FastAPI dev
         backend, which has no TLS. See res/xml/network_security_config.xml
         and context/conventions.md ("Running the phone app on the real
         backend over LAN") for why this is needed and why it's scoped to
         debug builds only. -->
    <application android:networkSecurityConfig="@xml/network_security_config" />
</manifest>
