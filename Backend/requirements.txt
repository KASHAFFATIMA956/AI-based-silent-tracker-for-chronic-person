fastapi==0.141.1
uvicorn[standard]==0.52.4
sqlalchemy==2.0.52
alembic==1.19.1
psycopg2-binary==2.9.12
pydantic-settings==2.15.0
python-dotenv==1.2.3
bcrypt==5.0.0
PyJWT==2.13.0
anthropic==1.0.0
python-multipart==0.0.20
# app/services/audio_analysis.py — numpy only (NOT librosa, see that
# module's docstring for why: librosa's numba/llvmlite/scipy/scikit-learn
# dependency chain proved a poor fit for this sandbox's network
# conditions during development). numpy has no heavy compiled transitive
# dependencies of its own. Audio DECODING (m4a/aac/etc -> PCM) is done by
# shelling out to the system `ffmpeg` binary, not a Python package — that
# binary must be present on PATH wherever this backend runs (already
# present on the dev machine used to build/verify this).
numpy==2.5.2
